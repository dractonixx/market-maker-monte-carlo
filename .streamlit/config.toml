# The charts are drawn on a light surface (plots.py), so the app matches them.
[theme]
base = "light"
backgroundColor = "#fcfcfb"
secondaryBackgroundColor = "#f0efec"
textColor = "#0b0b0b"
primaryColor = "#2a78d6"

[browser]
gatherUsageStats = false
